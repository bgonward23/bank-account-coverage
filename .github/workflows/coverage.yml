name: C++ Unit Tests & Coverage

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-12 g++-12 cmake
        pip install gcovr

    # ========================================================
    # ROUND 1: COPILOT ONLY
    # ========================================================
    - name: 🧪 Run Round 1 (Copilot)
      run: |
        echo "=============================================" > comparison_results.txt
        echo "📊 THESIS COMPARISON REPORT (Isolated Builds)" >> comparison_results.txt
        echo "=============================================" >> comparison_results.txt
        
        rm -rf build
        cmake -S . -B build -DBUILD_COPILOT=ON -DCMAKE_CXX_COMPILER=g++-12
        cmake --build build
        
        ./build/test_lru || true
        ./build/test_threadpool || true
        ./build/test_bank || true
        
        echo "--- COPILOT SCORES ---" >> comparison_results.txt
        echo "📋 LINE COVERAGE:" >> comparison_results.txt
        gcovr -r . -f src/ --print-summary >> comparison_results.txt
        echo -e "\n🌿 BRANCH COVERAGE:" >> comparison_results.txt
        gcovr -r . -f src/ --branches >> comparison_results.txt

    # ========================================================
    # ROUND 2: GPT-4o ONLY
    # ========================================================
    - name: 🧪 Run Round 2 (GPT-4o)
      run: |
        # NUKE THE BUILD FOLDER (This is the secret to getting 87 lines)
        rm -rf build 
        
        cmake -S . -B build -DBUILD_GPT4O=ON -DCMAKE_CXX_COMPILER=g++-12
        cmake --build build
        
        ./build/test_lru_3agent || true
        ./build/test_threadpool_3agent || true
        ./build/test_bank_3agent || true
        
        echo -e "\n--- GPT-4o SCORES ---" >> comparison_results.txt
        echo "📋 LINE COVERAGE:" >> comparison_results.txt
        gcovr -r . -f src/ --print-summary >> comparison_results.txt
        echo -e "\n🌿 BRANCH COVERAGE:" >> comparison_results.txt
        gcovr -r . -f src/ --branches >> comparison_results.txt

    # ========================================================
    # ROUND 3: QWEN ONLY
    # ========================================================
    - name: 🧪 Run Round 3 (Qwen)
      run: |
        # NUKE THE BUILD FOLDER AGAIN
        rm -rf build
        
        cmake -S . -B build -DBUILD_QWEN=ON -DCMAKE_CXX_COMPILER=g++-12
        cmake --build build
        
        ./build/test_lru_qwen || true
        ./build/test_threadpool_qwen || true
        ./build/test_bank_qwen || true
        
        echo -e "\n--- QWEN SCORES ---" >> comparison_results.txt
        echo "📋 LINE COVERAGE:" >> comparison_results.txt
        gcovr -r . -f src/ --print-summary >> comparison_results.txt
        echo -e "\n🌿 BRANCH COVERAGE:" >> comparison_results.txt
        gcovr -r . -f src/ --branches >> comparison_results.txt
        
        # PRINT FINAL REPORT
        echo "#############################################"
        echo "      🏆 FINAL THESIS COMPARISON REPORT      "
        echo "#############################################"
        cat comparison_results.txt
